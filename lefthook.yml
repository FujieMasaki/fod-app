pre-commit:
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs,mts,cts}"
      run: pnpm exec eslint -- {staged_files}

pre-push:
  parallel: true
  commands:
    types:
      run: pnpm type-check
    tests:
      run: pnpm test
