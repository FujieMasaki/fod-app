# MVP-010: 録音前の説明と音声の受け渡し・中断UX

| 項目 | 内容 |
| --- | --- |
| ID | MVP-010 |
| タスク名 | 録音前の説明と音声の受け渡し・中断UX |
| 対象領域 | Frontend |
| 作業区分 | 実装 |
| 優先度 | P1 |
| 状態 | Blocked |

## 目的と作業範囲

既存のMediaDevices / MediaRecorderによる録音を拡張し、利用者への事前説明と実サービスで必要な入力の受け渡しを実装する。durationだけを渡す現行動作を、決定した音声利用方式に合わせる。生成開始後の状態表示と再試行はMVP-011で扱う。

## 確認可能な完了条件

- [ ] 録音開始前に音声の送信有無・送信先・保存と削除の扱いを確認でき、説明と実際の処理が一致する。
- [ ] マイク権限拒否・非対応を成功した録音として扱わず、次の操作を案内する。対応ブラウザと録音中断時の振る舞いをPlanで明確にする。
- [ ] 録音成功・停止・中断を区別し、決定した音声入力とdurationを生成フローへ受け渡す。必要な形式・サイズの制限を利用者へ示す。
- [ ] 二重停止防止とstream / AudioContext解放の既存動作を維持し、音声Blob・文字起こしをbrowserへ永続化しない。
- [ ] 受け渡せる音声がない場合を明示し、関連するjournalingの現行挙動を更新している。

## 依存するタスクID

- MVP-002
- MVP-003
- MVP-005
- MVP-007

依存関係の上流にある設計判断・API契約が未確定のためBlocked。確定後は依存先の提供状況を確認して着手する。

## 根拠となる仕様書と見出し

- [journaling.md](../journaling.md) — 「1. 現在実装されているflow」
- [journaling.md](../journaling.md) — 「2. データと正本」
- [journaling.md](../journaling.md) — 「4. 実サービスのMVP受け入れ条件」
- [product.md](../product.md) — 「4. MVP対象外と実装前に決めること」

## 必要な検証

- 対応対象のブラウザで、初回権限許可・拒否・非対応・停止・中断・画面離脱を手動確認。
- 受け渡す入力とduration、制限違反、二重停止、録音資源解放をテストし、browser storageへ音声・文字起こしが残らないことを確認。

## 関連Implementation Plan

未作成。着手時にAGENTS.mdの規約に従って作成し、ここへリンクを追記する。
